name: Jekyll site CI

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: docs/master
    - name: Update this Site
      run: |
        cd /dnmp/www/docs \
        git pull
