name: docs  CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: docs CI
      uses: burnett01/rsync-deployments@4.1
      with:
        # 这里是 rsync 的参数 switches: -avzh --delete --exclude="" --include="" --filter=""
        switches: -avzh
        # path: public/
        remote_path: /dnmp/www/docs
        remote_host: docs.sreio.com
        remote_port: 22
        remote_user: root
        remote_key: ${{ secrets.MY_UBUNTU_RSA_PRIVATE_KEY }}

